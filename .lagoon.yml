definitions:
  cronjobs:
    - &default-cron-5
      name: default cron 5
      schedule: "*/5 * * * *"
      command: drush --root=/app cron
      service: cli
    - &default-cron-15
      name: default cron 15
      schedule: "*/15 * * * *"
      command: drush --root=/app cron
      service: cli
    - &default-cron-native
      name: default cron native
      schedule: "0 * * * *"
      command: drush cron
      service: cli

docker-compose-yaml: docker-compose.yml

project: drupal8-example-advanced

tasks:
#   pre-rollout:
#     - run:
#         name: drush sql-dump
#         command: mkdir -p /app/web/sites/default/files/private/ && drush sql-dump --ordered-dump --gzip --result-file=/app/web/sites/default/files/private/pre-deploy-dump.sql.gz || true
#         service: cli

  post-rollout:
    # - run:
    #     name: drush cim
    #     command: drush -y cim
    #     service: cli
    - run:
        name: drush updb
        command: drush -y updb
        service: cli
    - run:
        name: drush cr
        command: drush -y cr
        service: cli

environments:
  main:
    routes:
      # nginx.main.salvo-d8.de3.amazee.io
      - nginx:
        - main-salvo-d8.duckdns.org:
            tls-acme: "true"
            insecure: Redirect
            annotations:
              nginx.ingress.kubernetes.io/server-snippet: |
                add_header X-branch "#main";
        - main-ch1-salvo-d8.duckdns.org:
            tls-acme: "true"
            insecure: Redirect
            annotations:
              nginx.ingress.kubernetes.io/server-snippet: |
                add_header X-branch "#main";                
    cronjobs:
      - *default-cron-5
      - *default-cron-15
      - *default-cron-native
      - name: drush cron main native
        schedule: "0 * * * *"
        command: drush cron
        service: cli

  stage:
    routes:
      # nginx.stage.salvo-d8.de3.amazee.io
      - nginx:
        - stage-salvo-d8.duckdns.org:
            tls-acme: "true"
            insecure: Redirect
            annotations:
              nginx.ingress.kubernetes.io/server-snippet: |
                add_header X-branch "#stage";
    cronjobs:
      - *default-cron-5
      - *default-cron-15
      - *default-cron-native 
      - name: ls env stage native
        schedule: "0 * * * *"
        command: ls
        service: cli      